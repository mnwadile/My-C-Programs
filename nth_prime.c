#include<stdio.h>
#include<math.h>

void main()
{
    int i,j,n,m,c=0;
    printf("Enter Number to find prime number\n");
    scanf("%d",&n);
    // m= sqrt(n);
    //printf("%d",m);

    while(c!=n)
    {
        if(n%i == 0)
        {
            c=c+1;
        }

    }
}
